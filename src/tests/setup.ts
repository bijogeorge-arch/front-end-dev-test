/**
 * @file setup.ts
 * @description Vitest global setup file.
 * Imports jest-dom so all tests can use matchers like
 * `toBeInTheDocument`, `toHaveClass`, `toHaveValue`, etc.
 */
import '@testing-library/jest-dom';
